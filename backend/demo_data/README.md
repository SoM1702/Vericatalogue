# Synthetic demo data

All files in this folder are generated locally by `app/demo_assets.py` when the backend starts. They are intentionally fabricated for VeriCatalog Proof and are not supplier catalog data. They exist only to exercise the evidence, normalization, conflict, export, and catalog-health workflows; they must not be used to make real-world accuracy or business-impact claims.
